---
description: 兰芯密钥真相源在 ~/.config/lanxin，禁止把 env 主存储放在 myCode
alwaysApply: true
---

# 兰芯密钥 / Agent Init

开始涉及 Cloudflare、阿里云、第三方 API 密钥前，先读：

`~/.config/lanxin/AGENTS.md`

## 规则

- **真相源**：`~/.config/lanxin/env/`（不在 `~/myCode`）
- 本仓库 `.env` 是指向 `~/.config/lanxin/env/projects/lan-web-homepage.env` 的软链
- Cloudflare 日常用 `CLOUDFLARE_API_TOKEN`；创建/轮换 Token 用 `CLOUDFLARE_BOOTSTRAP_API_TOKEN`
- 阿里云 OSS 用 `ALIYUN_*`（见 `docs/oss.md` 与 `aliyun-oss.mdc`）
- **禁止**把 Secret 写入源码、提交、PR 或完整复述到聊天
- 加载：`source ~/.config/lanxin/bin/load-env.sh project:lan-web-homepage`
