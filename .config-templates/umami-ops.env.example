# Copy to ~/.config/lanxin/env/umami/ops.env (chmod 600). Never commit real values.
# `npm run umami:apply` can create this file if it is missing.

POSTGRES_PASSWORD=
APP_SECRET=
UMAMI_ADMIN_USERNAME=admin
UMAMI_ADMIN_PASSWORD=
UMAMI_ACCESS_EMAIL_DOMAIN=lancloudtech.com
UMAMI_ACCESS_EMAILS=
